const log = console.log

const id = document.getElementById.bind(document)
const q = document.querySelector.bind(document)
const all = document.querySelectorAll.bind(document)

const URL = q("meta[name='baseURL']").content